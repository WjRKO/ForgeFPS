name: build
on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - run: python -m pip install --upgrade pip pyinstaller

      - name: Build onedir
        run: pyinstaller --onedir --name forgefps-agent --console --noupx --clean --version-file version_info.txt forgefps_agent.py

      - name: Zip folder
        shell: pwsh
        run: Compress-Archive -Path 'dist/forgefps-agent' -DestinationPath 'dist/forgefps-agent.zip' -Force

      - id: hash
        shell: pwsh
        run: |
          $h = (Get-FileHash dist/forgefps-agent.zip -Algorithm SHA256).Hash.ToLower()
          echo "SHA256=$h" >> $env:GITHUB_STEP_SUMMARY
          echo "SHA256=$h" >> $env:GITHUB_OUTPUT

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/forgefps-agent.zip
          generate_release_notes: true
          body: |
            **SHA256 (ZIP):** `${{ steps.hash.outputs.SHA256 }}`

            v0.6.7 — Build --onedir (folder + DLLs). Uso: estrai il ZIP e lancia forgefps-agent.exe --token IL_TUO_TOKEN
